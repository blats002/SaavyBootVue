spring.application.name=SpringBootVue

spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
spring.liquibase.enabled=true

# MySQL database connection properties
spring.datasource.url=jdbc:mysql://mysql:3306/saavy_db
spring.datasource.username=mabelLocal
spring.datasource.password=Tinals00a

# Hibernate JPA settings
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Additional MySQL performance settings (optional)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
